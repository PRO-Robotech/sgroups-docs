---
id: namespaces
sidebar_position: 2
title: Namespaces
---

import { DICTIONARY } from '@site/src/constants/dictionary'
import { TYPES } from '@site/src/constants/types'
import { RESTRICTIONS } from '@site/src/constants/restrictions'
import { Restrictions } from '@site/src/components/commonBlocks/Restrictions'
import { Codes } from '@site/src/components/commonBlocks/Codes'
import { ApiOperation } from '@site/src/components/commonBlocks/ApiOperation'
import CodeBlock from '@theme/CodeBlock'
import dedent from 'ts-dedent'

# Namespaces

{DICTIONARY.resourceNamespace.full}

---

## Upsert

<ApiOperation method="POST" endpoint="/v1/namespaces/upsert">

Создает новое пространство имен или обновляет существующее.

#### Тело запроса

<table>
  <thead>
    <tr>
      <th>Параметр</th>
      <th>Обязательность</th>
      <th>Тип</th>
      <th>Описание</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>metadata.name</code></td>
      <td>да</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.name.short}</td>
    </tr>
    <tr>
      <td><code>metadata.labels</code></td>
      <td>нет</td>
      <td><code>{TYPES.mapStringString}</code></td>
      <td>{DICTIONARY.labels.short}</td>
    </tr>
    <tr>
      <td><code>metadata.annotations</code></td>
      <td>нет</td>
      <td><code>{TYPES.mapStringString}</code></td>
      <td>{DICTIONARY.annotations.short}</td>
    </tr>
    <tr>
      <td><code>spec.displayName</code></td>
      <td>нет</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.displayName.short}</td>
    </tr>
    <tr>
      <td><code>spec.comment</code></td>
      <td>нет</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.comment.short}</td>
    </tr>
    <tr>
      <td><code>spec.description</code></td>
      <td>нет</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.description.short}</td>
    </tr>
  </tbody>
</table>

#### Пример запроса

<CodeBlock language="bash">
  {dedent`
    curl -X POST http://localhost:9006/v1/namespaces/upsert \\
      -H 'Content-Type: application/json' \\
      -d '{
        "namespaces": [
          {
            "metadata": {
              "name": "production",
              "labels": {
                "env": "prod"
              }
            },
            "spec": {
              "displayName": "Production",
              "comment": "Продуктивное окружение",
              "description": "Пространство имен для продуктивных сервисов"
            }
          }
        ]
      }'
  `}
</CodeBlock>

#### Пример ответа

<CodeBlock language="json">
  {dedent`
    {
      "namespaces": [
        {
          "metadata": {
            "uid": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "name": "production",
            "labels": {
              "env": "prod"
            },
            "creationTimestamp": "2025-01-15T10:00:00Z",
            "resourceVersion": "1"
          },
          "spec": {
            "displayName": "Production",
            "comment": "Продуктивное окружение",
            "description": "Пространство имен для продуктивных сервисов"
          }
        }
      ]
    }
  `}
</CodeBlock>

<Restrictions items={[
  { label: 'metadata / spec', rules: RESTRICTIONS.metadataRequired },
  { label: 'upsert batch', rules: RESTRICTIONS.upsertBatch },
  { label: 'metadata.name', rules: RESTRICTIONS.name },
  { label: 'spec.displayName', rules: RESTRICTIONS.displayName },
]} />

<Codes codes={['invalidArgument', 'notFound', 'internal']} />

</ApiOperation>

---

## Delete

<ApiOperation method="POST" endpoint="/v1/namespaces/delete">

Удаляет пространство имен по `uid` или по `name`.

#### Тело запроса

<table>
  <thead>
    <tr>
      <th>Параметр</th>
      <th>Обязательность</th>
      <th>Тип</th>
      <th>Описание</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>uid</code></td>
      <td>нет*</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.uid.short}</td>
    </tr>
    <tr>
      <td><code>name</code></td>
      <td>нет*</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.name.short}</td>
    </tr>
  </tbody>
</table>

> *Необходимо указать `uid` **или** `name`.

#### Удаление по uid

<CodeBlock language="bash">
  {dedent`
    curl -X POST http://localhost:9006/v1/namespaces/delete \\
      -H 'Content-Type: application/json' \\
      -d '{
        "namespaces": [
          {
            "metadata": {
              "uid": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
            }
          }
        ]
      }'
  `}
</CodeBlock>

#### Удаление по name

<CodeBlock language="bash">
  {dedent`
    curl -X POST http://localhost:9006/v1/namespaces/delete \\
      -H 'Content-Type: application/json' \\
      -d '{
        "namespaces": [
          {
            "metadata": {
              "name": "production"
            }
          }
        ]
      }'
  `}
</CodeBlock>

#### Пример ответа

<CodeBlock language="json">
  {dedent`
    {}
  `}
</CodeBlock>

<Restrictions items={[
  { label: 'delete', rules: RESTRICTIONS.deleteNamespace },
]} />

<Codes codes={['invalidArgument', 'notFound', 'internal']} />

</ApiOperation>

---

## List

<ApiOperation method="POST" endpoint="/v1/namespaces/list">

Возвращает список пространств имен с возможностью фильтрации.

#### Тело запроса

<table>
  <thead>
    <tr>
      <th>Параметр</th>
      <th>Обязательность</th>
      <th>Тип</th>
      <th>Описание</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>selectors[].fieldSelector.name</code></td>
      <td>нет</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.name.short}</td>
    </tr>
    <tr>
      <td><code>selectors[].labelSelector</code></td>
      <td>нет</td>
      <td><code>{TYPES.mapStringString}</code></td>
      <td>{DICTIONARY.labelSelector.short}</td>
    </tr>
  </tbody>
</table>

#### Пример запроса

<CodeBlock language="bash">
  {dedent`
    curl -X POST http://localhost:9006/v1/namespaces/list \\
      -H 'Content-Type: application/json' \\
      -d '{
        "selectors": [
          {
            "fieldSelector": {
              "name": "production"
            }
          }
        ]
      }'
  `}
</CodeBlock>

#### Поля ответа

<table>
  <thead>
    <tr>
      <th>Поле</th>
      <th>Тип</th>
      <th>Описание</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>resourceVersion</code></td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.resourceVersion.short}</td>
    </tr>
  </tbody>
</table>

#### Пример ответа

<CodeBlock language="json">
  {dedent`
    {
      "resourceVersion": "42",
      "namespaces": [
        {
          "metadata": {
            "uid": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "name": "production",
            "labels": {
              "env": "prod"
            },
            "creationTimestamp": "2025-01-15T10:00:00Z",
            "resourceVersion": "1"
          },
          "spec": {
            "displayName": "Production",
            "comment": "Продуктивное окружение",
            "description": "Пространство имен для продуктивных сервисов"
          }
        }
      ]
    }
  `}
</CodeBlock>

<Restrictions items={[
  { label: 'selectors', rules: RESTRICTIONS.selectors },
]} />

<Codes codes={['invalidArgument', 'internal']} />

</ApiOperation>

---

## Watch

<ApiOperation method="POST" endpoint="/v1/namespaces/watch">

Серверный стриминг — возвращает поток изменений пространств имен в реальном времени.

#### Тело запроса

<table>
  <thead>
    <tr>
      <th>Параметр</th>
      <th>Обязательность</th>
      <th>Тип</th>
      <th>Описание</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>selectors[].fieldSelector.name</code></td>
      <td>нет</td>
      <td><code>{TYPES.string}</code></td>
      <td>{DICTIONARY.name.short}</td>
    </tr>
    <tr>
      <td><code>selectors[].labelSelector</code></td>
      <td>нет</td>
      <td><code>{TYPES.mapStringString}</code></td>
      <td>{DICTIONARY.labelSelector.short}</td>
    </tr>
    <tr>
      <td><code>resource_version</code></td>
      <td>нет</td>
      <td><code>{TYPES.int64}</code></td>
      <td>{DICTIONARY.resourceVersion.short}</td>
    </tr>
  </tbody>
</table>

#### Пример запроса

<CodeBlock language="bash">
  {dedent`
    curl -X POST http://localhost:9006/v1/namespaces/watch \\
      -H 'Content-Type: application/json' \\
      -d '{
        "resourceVersion": "0"
      }'
  `}
</CodeBlock>

#### Пример ответа

<CodeBlock language="json">
  {dedent`
    {
      "type": "MODIFIED",
      "namespaces": [
        {
          "metadata": {
            "uid": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "name": "production",
            "labels": {
              "env": "prod"
            },
            "creationTimestamp": "2025-01-15T10:00:00Z",
            "resourceVersion": "2"
          },
          "spec": {
            "displayName": "Production",
            "comment": "Продуктивное окружение (обновлено)",
            "description": "Пространство имен для продуктивных сервисов"
          }
        }
      ]
    }
  `}
</CodeBlock>

<Restrictions items={[
  { label: 'selectors', rules: RESTRICTIONS.selectors },
]} />

<Codes codes={['invalidArgument', 'internal']} />

</ApiOperation>
